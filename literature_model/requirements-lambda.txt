# Lambda-specific dependencies (excludes heavy packages)
# sentence-transformers (~1.5GB) excluded — CLIP runs during aggregation only
# matplotlib, networkx excluded — batch-only steps
# openai removed — LLM extraction uses Bedrock Claude Sonnet 4.6 via boto3
python-dotenv==1.2.1
pydantic>=2.0,<3.0
pypdf==6.7.1
pdfplumber==0.11.9
pymupdf==1.24.14
nltk==3.9.2
numpy<2.1
pandas==2.3.3
pyarrow<19
boto3==1.42.26
